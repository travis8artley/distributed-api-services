# Contributing

## Development Loop
1. Add handlers with explicit contract tests.
2. Run `go test ./...` and `go vet ./...`.
3. Keep router registrations and docs in sync.

## Commit Style
Use focused commits with conventional prefixes.
